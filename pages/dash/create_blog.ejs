<%- include('./_layouts/header') %>


<div class="container mt-5">
    <!-- Page Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="text-primary">Create Blog Post</h2>
        <a href="/admin/manage-blogs" class="btn btn-secondary"><i class="fa fa-arrow-left"></i> Back to Manage Blogs</a>
    </div>

    <!-- Blog Form -->
    <form action="" method="" enctype="multipart/form-data">
        <!-- Blog Title Row -->
        <div class="form-row">
            <label for="title">Blog Title</label>
            <input type="text" id="bTitle" name="bTitle" class="form-control" placeholder="Enter blog title" required>
        </div>

        <!-- Category Row -->
        <div class="form-row">
            <label for="category">Category</label>
            <select id="category"  class="form-control" name="category" required>
                <option value="">-- Select Category --</option>
                <option value="Travel Tips">Travel Tips</option>
                <option value="Destinations">Destinations</option>
                <option value="Special Offers">Special Offers</option>
            </select>
        </div>

        <!-- Featured Image Row -->
        <div class="form-row">
            <label for="featuredImage">Featured Image/Thumbnail</label>
            <input type="file" id="featuredImage" name="featuredImage" class="form-control" accept="image/*">
        </div>

        <!-- Blog Content Row -->
        <div class="form-row">
            <label for="blogContent">Blog Content</label> <br>
            <textarea id="blogContent" name="blogContent" class="form-control" rows="14"></textarea>
        </div>

        <!-- SEO Metadata Section -->
        <h5 class="mb-3 mt-4">SEO Metadata</h5>
        <div class="form-row">
            <label for="metaTitle">Meta Title</label>
            <input type="text" id="metaTitle" name="metaTitle" class="form-control" placeholder="Enter meta title" required>
        </div>
        <div class="form-row">
            <label for="metaDescription">Meta Description</label>
            <input type="text" id="metaDescription" name="metaDescription" class="form-control" placeholder="Enter meta description" required>
        </div>
        <div class="form-row">
            <label for="keywords">Keywords</label>
            <input type="text" id="keywords" name="keywords" class="form-control" placeholder="E.g., Travel, Safari, Offers">
        </div>

        <!-- Blog Status -->
        <div class="form-row">
            <label class="form-label mb-2">Status:</label>
            <div class="form-check form-check-inline">
                <input class="form-check-input" type="radio" name="status" id="draft" value="Draft" checked>
                <label class="form-check-label" for="draft">Draft</label>
            </div>
            <div class="form-check form-check-inline">
                <input class="form-check-input" type="radio" name="status" id="published" value="Published">
                <label class="form-check-label" for="published">Publish</label>
            </div>
        </div>

        <!-- Action Buttons -->
        <div class="d-flex gap-2 mt-4">
            <button type="submit" class="btn btn-success"><i class="fa fa-save"></i> Save Blog</button>
            <button type="button" class="btn btn-primary"><i class="fa fa-eye"></i> Preview</button>
            <button type="reset" class="btn btn-warning"><i class="fa fa-eraser"></i> Clear Form</button>
        </div>
    </form>
</div>



<script src="https://cdn.tiny.cloud/1/wz6upinpprryclrsrruprdr6wyrcdymnv8lx75xu8t7q5qa9/tinymce/7/tinymce.min.js" referrerpolicy="origin"></script>

<script>
    document.querySelector('.btn-preview').addEventListener('click', function() {
        const title = document.getElementById('blogContent').value;
        const content = tinymce.get('blogContent').getContent(); // Replace 'content' with your editor's actual ID if different

        if (title && content) {
            document.getElementById('previewContainer').innerHTML = `
                <h2>Preview:</h2>
                <h3>${title}</h3>
                <div>${content}</div>
            `;
            alert(`Preview:\n\nTitle: ${title}\n\nContent:\n${content}`);
        } else {
            alert("Please enter both title and content to preview.");
        }
    });


    tinymce.init({
        selector: '#blogContent', // Target your content textarea
        plugins: 'image link media lists code',
        toolbar: 'undo redo | bold italic | alignleft aligncenter alignright | bullist numlist outdent indent | link image media | code',
        height: 400,
        image_title: true,
        automatic_uploads: true,
        file_picker_types: 'image',
        file_picker_callback: function (cb, value, meta) {
            const input = document.createElement('input');
            input.setAttribute('type', 'file');
            input.setAttribute('accept', 'image/*');
            
            input.onchange = function () {
                const file = this.files[0];
                const reader = new FileReader();
                
                reader.onload = function () {
                    const id = 'blobid' + new Date().getTime();
                    const blobCache = tinymce.activeEditor.editorUpload.blobCache;
                    const base64 = reader.result.split(',')[1];
                    const blobInfo = blobCache.create(id, file, base64);
                    blobCache.add(blobInfo);

                    // Provide the image URL to the callback and set the image title as the file name
                    cb(blobInfo.blobUri(), { title: file.name });
                };

                reader.readAsDataURL(file);
            };

            input.click();
        },
        content_style: "body { font-family:Helvetica,Arial,sans-serif; font-size:14px }", // Optional: set content style for better readability
    });

</script>

<%- include('./_layouts/footer') %>